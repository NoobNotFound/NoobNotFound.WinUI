<Flyout
    x:Class="NoobNotFound.WinUI.Common.UI.Controls.TenorFlyout"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NoobNotFound.WinUI.Common.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:NoobNotFound.WinUI.Common.Models"
    mc:Ignorable="d">

    <Grid Margin="-12">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="txtSearch" TextChanged="txtSearch_TextChanged" HorizontalAlignment="Stretch" Grid.Row="0" PlaceholderText="Search Tenor GIFs"/>
        <GridView ItemsSource="{x:Bind GIFs,Mode=OneWay}" ItemClick="GridView_ItemClick" SelectionMode="None" Grid.Row="2" Margin="0,5,0,0">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:TenorGIF" x:DefaultBindMode="OneWay">
                    <Image MaxWidth="100" Height="60" Source="{x:Bind ImageSource}" ToolTipService.ToolTip="{x:Bind TagsString}"/>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Flyout>